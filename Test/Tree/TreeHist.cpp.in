#include <Test/Common/Hist.h>

TEST_F(Aggregator_${TYPE}, HistFromTree)
{
	DECL_PH_SET_${TYPE};
	TestHistFromTree(*m_tree, m_class, std::make_tuple(class_, number, name, exam, math, japanese, english, science, social));
}