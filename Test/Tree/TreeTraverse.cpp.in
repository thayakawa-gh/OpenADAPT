#include <Test/Common/Traverse.h>

#define T_${TYPE}

TEST_F(Aggregator_${TYPE}, TraverserIncr)
{
	DECL_PH_SET_${TYPE};
	TestTraverserIncr(*m_tree, m_class, layer0, layer1, layer2);
}
TEST_F(Aggregator_${TYPE}, TraverserDecr)
{
	DECL_PH_SET_${TYPE};
	TestTraverserDecr(*m_tree, m_class, layer0, layer1, layer2);
}
TEST_F(Aggregator_${TYPE}, TraverserMoveForward)
{
	DECL_PH_SET_${TYPE};
	TestTraverserMoveForward(*m_tree, m_class, layer0, layer1, layer2);
}
TEST_F(Aggregator_${TYPE}, TraverserMoveBackward)
{
	DECL_PH_SET_${TYPE};
	TestTraverserMoveBackward(*m_tree, m_class, layer0, layer1, layer2);
}