#include <Test/Common/Traverse.h>

#define T_${TYPE}

TEST_F(Aggregator_${TYPE}, TraverserIncr)
{
	DECL_PH_SET_${TYPE};
	TestTraverserIncr(*m_table, m_class, layer0);
}
TEST_F(Aggregator_${TYPE}, TraverserDecr)
{
	DECL_PH_SET_${TYPE};
	TestTraverserDecr(*m_table, m_class, layer0);
}